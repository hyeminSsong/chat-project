# 이 파일을 복사해 .env 로 저장한 뒤 값을 채워서 사용하세요.
# .env는 .gitignore에 의해 커밋되지 않습니다.

# --- PostgreSQL ---
POSTGRES_DB=test_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5432

# --- Spring Boot (chat-api) ---
BACKEND_PORT=8080

# --- React (frontend) ---
FRONTEND_PORT=5173
